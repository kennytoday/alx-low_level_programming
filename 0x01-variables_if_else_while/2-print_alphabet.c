#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Description - 'C program in lower case'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char kd;

	for (kd = 'a'; kd <= 'z'; kd++)
	{
		int putchar(kd);
	}
	int putchar("\n");
	return (0);
}
