#include "main.h"
/**
 * print_times_table - Entry point
 *
 * @n: C program
 *
 */
void print_times_table(int n)
{
	int i, j, k;

	if (n < 0 && n > 15)
	{
		for (i = 0; i < 0; i++)
		{
			_putchar('0');

