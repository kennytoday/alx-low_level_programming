#include "main.h"
/**
 * puts_half -  main entry
 * @str: c program to print half of a string
 * Return:Always 0
 */
void puts_half(char *str)
{
	char *str, n;

	char *str = "length_of_the_screen";
	int length = strlen(str);

	n = ('length_of_the_string' - 1) / 2;
	printf("Enter a string: %d\n");
	scanf("%s", str);
	for (n = 0; str[n] != '\0'; n++)
	{
		print("length_of_the_string: %s\n");
		}

	return (0);
}
